{
  "name": "express-intro",
  "version": "1.0.0",
  "description": "Working on Express framework for Project 3",
  "main": "index.js",
  "scripts": {
    "start": "node index",
    "dev": "nodemon index",
    "create_db": "psql -U postgres -f sql/create_db.sql",
    "create_tables": "psql -U postgres -d mr_coffee -f sql/create_tables.sql",
    "seed_tables": "psql -U postgres -d mr_coffee -f sql/seed_tables.sql",
    "setup_db": "npm run create_db && npm run create_tables && npm run seed_tables",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Vikram Duvasi",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "body-parser": "^1.19.0",
    "ejs": "^3.1.6",
    "express": "^4.17.1",
    "morgan": "^1.10.0",
    "pg-promise": "^10.11.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.13"
  }
}
